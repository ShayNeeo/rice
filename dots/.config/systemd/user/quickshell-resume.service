[Unit]
Description=Restart Quickshell after sleep/suspend
After=sleep.target suspend.target
PartOf=graphical-session.target

[Service]
Type=oneshot
ExecStart=systemctl --user start quickshell.service

[Install]
WantedBy=sleep.target suspend.target
